## Activation Keywords — context-service

- Context Service, Context Definition, Context Mapping, Context Hydration
- Context Instance, Context Tag, Context Filter, Build Context
- Context Service Admin, Context Service Runtime
- Pricing Procedure context, RCA context definition
- Build Context invocable action, Query Context, Update Context, Persist Context, Delete Context
- context cache between test runs, context service limits
- extend a context definition, migrate context definition, clone context definition
- context definition lifecycle, activate context definition, package context definition
- context intents, context mapping intents, context filter types
- context service concepts, context service architecture

## Request Routing — context-service

| User says something like... | Use this command |
|-----------------------------|-----------------|
| "how does Context Service work", "explain Context Definition", "what is Context Hydration" | `/revenue-cloud-q3:context-service` |
| "set up a Context Definition", "context mapping configuration", "context filters" | `/revenue-cloud-q3:context-service` |
| "test a flow with Build Context", "context cache between test runs", "invocable actions for context" | `/revenue-cloud-q3:context-service` |
| "extend a context definition", "migrate context definition", "context service limits" | `/revenue-cloud-q3:context-service` |
| "RCA pricing procedure uses context service", "context for billing calculations" | `/revenue-cloud-q3:context-service` |
| "Build Context action", "Query Context action", "Persist Context action", "Delete Context action" | `/revenue-cloud-q3:context-service` |
| "context definition lifecycle", "activate context definition", "clone context definition" | `/revenue-cloud-q3:context-service` |

## Key Command

`/mfg:context-service` — Context Service domain knowledge: concepts, configuration, invocable actions, test patterns, limits, and migration guidance
