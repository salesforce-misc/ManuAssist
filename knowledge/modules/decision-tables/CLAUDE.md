## Activation Keywords — decision-tables

- Decision Tables, business rules, decision table input, decision table output
- Dataset Link, Group By decision table, outcome sorting, Run Decision Tables
- Invoke Decision Table, Refresh Decision Table, decision table flow action
- discount rules, rule evaluation, pricing rules, rebate rules
- decision table source object, decision table wizard, activate decision table
- decision table outcome variable, decision table result variable
- decision table limits, invocation rate limit, decision table rows limit
- decision table gotchas, decision table known issues
- create a decision table, configure decision table, decision table setup

## Request Routing — decision-tables

| User says something like... | Use this command |
|-----------------------------|-----------------|
| "create a decision table", "set up discount rules", "business rules evaluation" | `/revenue-cloud-q3:decision-tables` |
| "how does decision table work", "explain dataset link", "Group By in decision tables" | `/revenue-cloud-q3:decision-tables` |
| "invoke decision table in flow", "refresh decision table", "decision table outcomes" | `/revenue-cloud-q3:decision-tables` |
| "decision table limits", "how many rules can a decision table read", "invocation rate limit" | `/revenue-cloud-q3:decision-tables` |
| "decision table source object", "decision table wizard", "activate decision table" | `/revenue-cloud-q3:decision-tables` |
| "dataset link decision table", "link dataset to decision table", "dataset link type compatibility" | `/revenue-cloud-q3:decision-tables` |
| "outcome variable decision table", "decision table result in flow", "decision table output" | `/revenue-cloud-q3:decision-tables` |

## Key Command

`/mfg:decision-tables` — Decision Tables domain knowledge: business rules design, create/configure, dataset links, Group By, invoke in Flow, refresh, limits, and gotchas
